server:
  servlet:
    context-path: /petz/app

spring:  datasource:    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:petz-test
    name: sa
    password: 
  jpa:    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:      ddl-auto: create  h2:    console:      enabled: true
      path: /db-console

      